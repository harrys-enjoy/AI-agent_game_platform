.env
.env.*.tmp
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# 실행 산출물 - 실제 Google OAuth 토큰/SQLite DB(.runtime/gmail_access_token.txt,
# google_credentials.sqlite3 등)가 여기 쌓인다. 커밋하면 자격증명이 그대로 GitHub에 올라간다.
.runtime/
runtime/
*.log
*.err
*.out

# 각자 로컬 전용 Google OAuth 클라이언트 시크릿/토큰 - 실제 자격증명이라 커밋 금지.
# 각 개발자가 이 폴더를 직접 만들어 client_secret.json/token.json을 넣어야 한다.
google-oauth-test/
